# Layer Organizer for Substance 3D Painter

레이어에 물린 제네레이터/필터 리소스를 분석해서 자동으로 이름을 붙여주는 Substance 3D Painter 플러그인입니다.
이미지 export 없이 레이어 스택 메타데이터만 조회하기 때문에 레이어가 수십 개여도 빠르게 처리됩니다.

## 주요 기능

- 마스크에 물린 이펙트(제네레이터/필터)의 실제 리소스 이름을 기준으로 분류하므로, 사람이 레이어 이름을 뭐라고 바꿔놨든 정확하게 인식합니다.
- 이펙트가 하나면 그 이름 그대로(`AO`, `그런지`), 여러 개가 겹치면 대표 이펙트 + `활용` 접미사(`그런지활용`, `커버쳐활용`)를 붙입니다.
- **같은 리소스를 파라미터만 다르게 쓰는 경우까지 구분**합니다.
  - 라이트: 수직 각도(Vertical Angle) 기준으로 `상단라이트` / `하단라이트`
  - 커버쳐: Global Balance(+ Invert 보정) 기준으로 `넓은엣지` / `얇은엣지`
  - 커버쳐 2개를 Subtract로 조합해 넓은 영역만 남긴 구성은 `넓은면적`으로 별도 인식
  - 리소스가 같은 Dot 패턴이라도 레이어의 Base Color 밝기(HSV V값, 0.5 기준)로 `밝은땡땡이` / `어두운땡땡이` 구분
- 이펙트가 전혀 없는 Fill 레이어는 채우는 채널에 따라 자동으로 이름이 붙습니다.
  - 색상 채널(또는 여러 채널 혼합) → `베이스`
  - 러프니스만 → `러프니스`, 노말만 → `노말`, 메탈릭만 → `메탈릭`, 하이트만 → `하이트`, 오퍼시티만 → `오퍼시티`, 이미시브만 → `이미시브`
- **손으로 직접 칠한 이펙트(Paint)가 섞여 있으면 자동 분류를 보류**하고 원래 이름을 유지합니다 (보정 필터만 보고 잘못 이름 짓는 것을 방지).
- **미리보기 모드**: 실제로 이름을 바꾸기 전에 어떻게 바뀔지 로그로 먼저 확인할 수 있습니다.
- **처리 범위 선택**: 전체 레이어 / 선택한 레이어만 / 선택한 레이어 제외 중 골라서 실행할 수 있습니다. 폴더를 선택하면 하위 레이어까지 포함됩니다.
- 폴더 안에 중첩된 레이어까지 재귀적으로 처리합니다.

## 설치 방법

1. 이 저장소의 `layer_organizer` 폴더를 통째로 복사합니다.
2. Substance 3D Painter의 Python 플러그인 폴더에 붙여넣습니다.
   - **Windows**: `문서\Adobe\Adobe Substance 3D Painter\python\plugins\`
   - **macOS**: `~/Documents/Adobe/Adobe Substance 3D Painter/python/plugins/`
   - Painter에서 `Python` 메뉴 → `Plugins Folder`를 클릭하면 정확한 경로가 바로 열립니다.
3. 최종 경로가 `.../python/plugins/layer_organizer/__init__.py`가 되어야 합니다.
4. Painter를 재시작하거나 `Python` 메뉴 → `Reload Plugins Folder`를 클릭합니다.
5. `Python` 메뉴에서 `layer_organizer`에 체크가 되어 있는지 확인합니다.

## 사용 방법

1. 프로젝트를 열고 "Layer Organizer" 도킹 패널을 확인합니다 (안 보이면 `Python` 메뉴 → `Layer Organizer 보이기` 클릭, 또는 `layer_organizer` 체크 해제 후 재체크).
2. 처리 범위를 선택합니다: 전체 레이어 / 선택한 레이어만 / 선택한 레이어 제외.
3. **"미리보기 (이름 변경 없이 확인만)"** 버튼을 눌러 어떻게 이름이 바뀔지 로그로 확인합니다.
4. 결과가 마음에 들면 **"실제 이름 적용"** 버튼을 눌러 실제로 반영합니다.

## 커스터마이징

`__init__.py` 상단의 키워드 사전을 수정해서 원하는 대로 확장할 수 있습니다.

- `CORE_KEYWORDS`: 레이어의 정체성을 결정하는 핵심 생성기 (AO, 커버쳐, 그런지 등)
- `MODIFIER_KEYWORDS`: 위 결과를 다듬기만 하는 보정 필터 (블러, 히스토그램 등)
- `PERSONAL_KEYWORDS`: 개인/스튜디오 워크플로우에서만 쓰는 리소스 이름을 여기에 추가하면 됩니다.
- `CHANNEL_LABELS`: Fill 레이어가 단일 채널만 채울 때 사용할 이름을 여기서 바꿀 수 있습니다.
- `REFINEMENT_FUNCS` / `refine_light` / `refine_curvature` / `refine_dots_by_brightness`: 같은 리소스를 파라미터만 다르게 쓰는 경우를 구분하는 규칙입니다. 프로젝트마다 파라미터 키 이름이나 임계값이 다를 수 있으니, Painter Python 콘솔에서 `src.get_parameters()`로 실제 키를 확인한 뒤 여기 함수를 수정하면 됩니다.

```python
PERSONAL_KEYWORDS = {
    "나뭇결": ["wood_grain", "grunge_lac"],
    "틈새": ["cavity"],
}
```

## 알려진 제한 사항

- 마스크 안의 이펙트만 분석합니다 (레이어 콘텐츠 자체에 걸린 필터는 현재 버전에서 다루지 않습니다).
- 손으로 직접 칠한 마스크(제네레이터/필터가 안 걸린 경우)는 자동 분류 대상이 아니며 원래 이름을 유지합니다.
- 같은 리소스를 파라미터만 바꿔서 다른 용도로 쓰는 경우(예: 커버쳐 리소스를 캐비티 모드로 바꿔 쓰는 경우)는 현재 리소스 이름만으로는 구분하지 못합니다.
- Substance 3D Painter의 Python API 세부 동작은 버전마다 다를 수 있습니다. 이슈가 발생하면 Painter 버전과 함께 제보해 주세요.

## 요구 사항

- Substance 3D Painter (Python API 지원 버전, PySide2 또는 PySide6 자동 감지)

## 변경 이력

### v1.2.0
- 파라미터 기반 세분화 추가: 라이트(상단/하단), 커버쳐(넓은/얇은엣지), 넓은면적(Subtract 조합 감지), 땡땡이(밝기 기반)
- 손으로 직접 칠한 이펙트(Paint)가 섞인 레이어는 오분류 방지를 위해 자동 분류 대상에서 제외

### v1.1.0
- Fill 레이어가 단일 채널만 채울 때 채널명 기반 자동 네이밍 추가 (러프니스, 노말, 메탈릭, 하이트, 오퍼시티, 이미시브)
- 처리 범위 선택 기능 추가 (전체 / 선택한 레이어만 / 선택한 레이어 제외)
- 패널 복구용 메뉴 액션 추가 (`Layer Organizer 보이기`)

### v1.0.0
- 최초 배포: 마스크 이펙트 기반 자동 네이밍, 미리보기/실제 적용 분리

## 라이선스

MIT License (LICENSE 파일 참고)
